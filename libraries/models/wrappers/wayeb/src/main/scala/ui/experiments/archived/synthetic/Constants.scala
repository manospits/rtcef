package ui.experiments.archived.synthetic

object Constants {
  val psaDir: String = System.getenv("PSARDIR")
  val wayebHome: String = System.getenv("WAYEB_HOME")
  val resultsDir: String = psaDir + "/results"
}
